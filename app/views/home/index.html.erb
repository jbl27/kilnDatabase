<% name = current_user.present? ? current_user.email : 'Stranger' %>

<h1>Welcome, <%= name %> </h1>
<% if user_signed_in? %>
  <%= link_to "Show my Profile", profile_path %>
<% else %>
  <p>Sign up using the button on the navigation bar above!</p>
<% end %>